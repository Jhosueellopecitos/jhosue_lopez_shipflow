spring:
  application:
    name: lopez_jhosue_shipflow

  datasource:
    url: jdbc:postgresql://localhost:5432/shipdb
    driver_class_name: org.postgresql.Driver
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
